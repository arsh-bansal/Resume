[phases.setup]
nixPkgs = ["nodejs-20_x", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile --prod=false --no-optional"]

[phases.build]
cmds = ["NODE_OPTIONS='--max-old-space-size=384' pnpm build"]

[start]
cmd = "NODE_OPTIONS='--max-old-space-size=256' pnpm start"

[variables]
NODE_ENV = "production"
NODE_OPTIONS = "--max-old-space-size=256"

